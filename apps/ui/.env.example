# Entorno de aplicación (dev|prod)
APP_ENV=dev

# Ruta absoluta opcional para la base de datos SQLite.
# Si no la defines, la app creará <DB_NAME>.db en el directorio HOME
# (en Android: sandbox interno de la app)
# SQLITE_DB_PATH=/storage/emulated/0/Android/data/com.mycompany.makingthegrade/files/makingthegrade.db

# Nombre lógico de la base (se usa para nombrar el .db si no defines SQLITE_DB_PATH)
DB_NAME=marking_grade

# Variables legacy (no requeridas actualmente; solo si migras a MySQL)
DB_HOST=192.168.137.1
DB_PORT=3306
DB_USER=tester
DB_PASS=changeme
DB_SSL=off

